export type CallVoiceType = {
    caller: string,
    receiver: string,
    conversation: string,
    video: string,
}

export type OfferVoiceType = {
    type: string,
    offer: any,
    receverId: any;
}